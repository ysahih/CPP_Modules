#include "ClapTrap.hpp"

int main (){

    ClapTrap claptrap("holly-molly!");
    
    claptrap.attack("slave");
    claptrap.takeDamage(10);
    claptrap.attack("slave");
    claptrap.attack("slave");
    claptrap.attack("slave");
    claptrap.attack("slave");
    claptrap.attack("slave");
    claptrap.attack("slave");
    claptrap.attack("slave");
    claptrap.attack("slave");
    claptrap.attack("slave");
    claptrap.attack("slave");
    claptrap.attack("slave");
    return 0;
}